# Danpla photos

Product photos for the **Danpla products** section live here as:

- `danpla-01.png` … `danpla-15.png`

To replace an image, keep the same filename or update the paths in `index.html` inside `#danpla`.
